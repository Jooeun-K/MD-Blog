export type PostDataType = {
  data: {
    date: string;
    description: string;
    id: string;
    postData: string;
    title: string;
  };
};

export type NavType = { id: string; title: string };
